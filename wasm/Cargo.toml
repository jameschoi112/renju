[package]
name = "renju-wasm"
version = "0.1.0"
edition = "2021"
description = "Renju/Gomoku AI core — WebAssembly build for JS"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
lto = true
opt-level = 3
